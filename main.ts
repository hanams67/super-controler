radio.onReceivedNumber(function (receivedNumber) {
    radio.sendValue("name", 0)
})
function go_forward (bool: boolean, text: string, list: any[], sprite: game.LedSprite, image: Image, num: number) {
    if (true) {
        basic.showNumber(randint(Math.round(0), 10))
    }
}
input.onButtonPressed(Button.A, function () {
    cuteBot.colorLight(cuteBot.RGBLights.ALL, 0xff0000)
})
radio.onReceivedString(function (receivedString) {
    basic.showNumber(randint(Math.round(0), 10))
})
radio.onReceivedValue(function (name, value) {
    if (joystick > 10) {
        cuteBot.moveTime(cuteBot.Direction.forward, 2, 5)
        joystickbit.Vibration_Motor(100)
        joystickbit.initJoystickBit()
    } else if (!((true as any) == (false as any) && ((false as any) == (true as any) || (true == true || false == false)))) {
    	
    }
})
let joystick = 0
turtle.forward(1)
control.inBackground(function () {
    joystick = joystick
    joystick += joystick
})
